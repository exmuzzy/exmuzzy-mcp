# Проверка Jira Structure REST API

## ✅ Текущая конфигурация

### Базовый путь API
```
/rest/structure/latest
```

### Используемые endpoints

1. **Получение списка структур**
   - Endpoint: `/structure`
   - Полный URL: `{BASE_URL}/rest/structure/latest/structure`
   - Метод: `GET`
   - Параметры: `?projectKey={projectKey}` (опционально)

2. **Получение деталей структуры**
   - Endpoint: `/structure/{structureId}`
   - Полный URL: `{BASE_URL}/rest/structure/latest/structure/{structureId}`
   - Метод: `GET`

3. **Получение элементов структуры**
   - Endpoint: `/structure/{structureId}/element`
   - Полный URL: `{BASE_URL}/rest/structure/latest/structure/{structureId}/element`
   - Метод: `GET`
   - Параметры: `?issueKey={key}&maxResults={n}&startAt={n}` (опционально)

4. **Получение иерархии структуры**
   - Endpoints (пробуются по порядку):
     1. `/structure/{structureId}/hierarchy`
     2. `/structure/{structureId}/element`
     3. `/structure/{structureId}/hierarchy?flat=false`
     4. `/structure/{structureId}/element?flat=false`
   - Полный URL: `{BASE_URL}/rest/structure/latest/structure/{structureId}/hierarchy`
   - Метод: `GET`
   - Параметры: `?issueKey={key}&maxResults={n}&startAt={n}` (опционально)

## 🔍 Как проверить работу API

### Вариант 1: Использование тестового скрипта

1. Установите переменные окружения:
```bash
export JIRA_BASE_URL="https://your-jira-instance.com"
export JIRA_BEARER_TOKEN="your-bearer-token"
# или
export JIRA_EMAIL="your-email"
export JIRA_API_TOKEN="your-api-token"
```

2. Запустите тест:
```bash
node test-structure-api.js
```

### Вариант 2: Прямая проверка через curl

```bash
# Проверка получения списка структур
curl -H "Authorization: Bearer YOUR_TOKEN" \
     -H "Accept: application/json" \
     https://your-jira-instance.com/rest/structure/latest/structure

# Или с Basic Auth
curl -u "email:api-token" \
     -H "Accept: application/json" \
     https://your-jira-instance.com/rest/structure/latest/structure
```

### Вариант 3: Использование MCP инструментов

Если MCP сервер запущен, можно использовать инструменты:
- `get_structures` - получить список структур
- `get_structure_details` - получить детали структуры
- `get_structure_hierarchy` - получить иерархию структуры
- `get_structure_elements` - получить элементы структуры

## ⚠️ Возможные проблемы

### Ошибка 404 (Not Found)

**Причины:**
1. Плагин Jira Structure не установлен или не активирован
2. API endpoint недоступен на этом инстансе Jira
3. Используется другая версия API

**Решения:**
- Проверьте, установлен ли плагин Jira Structure
- Попробуйте альтернативные пути:
  - `/rest/structure/1.0/structure`
  - `/rest/structure/2.0/structure`
- Проверьте документацию вашей версии плагина Structure

### Ошибка 403 (Forbidden)

**Причины:**
- Недостаточно прав доступа к Structure API
- Пользователь не имеет прав на просмотр структур

**Решения:**
- Проверьте права пользователя в Jira
- Убедитесь, что пользователь имеет доступ к проектам со структурами

### Ошибка 401 (Unauthorized)

**Причины:**
- Неверные учетные данные
- Токен истек или недействителен

**Решения:**
- Проверьте правильность токена/учетных данных
- Обновите токен, если необходимо

## 📝 Особенности реализации

1. **Fallback механизм**: При получении иерархии код пробует несколько вариантов endpoints, если один не работает
2. **Обработка ошибок**: Ошибки 404 обрабатываются как "нет данных", а не как критическая ошибка
3. **Гибкая обработка ответов**: Код поддерживает разные форматы ответов API (массив или объект с полем `structures`)

## 🔧 Альтернативные версии API

Если `/rest/structure/latest` не работает, можно попробовать:

1. **Версия 1.0**: `/rest/structure/1.0`
2. **Версия 2.0**: `/rest/structure/2.0`
3. **Без версии**: `/rest/structure` (если поддерживается)

Для изменения версии нужно изменить строку 92 в `src/jiraApiClient.ts`:
```typescript
const apiPath = useStructureApi 
  ? '/rest/structure/latest'  // Измените здесь
  : ...
```

## ✅ Статус проверки

- [x] Базовый путь API настроен: `/rest/structure/latest`
- [x] Endpoints определены корректно
- [x] Обработка ошибок реализована
- [x] Fallback механизм для иерархии работает
- [ ] Требуется проверка с реальным Jira инстансом (нужны переменные окружения)

## 📚 Дополнительная информация

- Документация Jira Structure API: https://docs.almworks.com/structure/latest/rest-api.html
- Код реализации: `src/jiraApiClient.ts` (строки 372-465)
- Сервис для работы со структурами: `src/services/structureService.ts`







