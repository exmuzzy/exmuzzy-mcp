# Руководство по установке оптимизированного Jira MCP Server

## 🎯 Цель

Установить и настроить оптимизированную версию Jira MCP Server для использования в Cursor с командой `/jira`.

**Результат:** Просмотр задач Jira за **1 запрос** вместо 6+ с ускорением **5-6x**.

---

## 📋 Предварительные требования

### 1. Установленное ПО

- **Node.js** 18.0.0 или выше
  ```bash
  node --version  # Проверка версии
  ```

- **npm** (поставляется с Node.js)
  ```bash
  npm --version
  ```

- **Cursor** (последняя версия)
  - Скачать: https://cursor.sh

### 2. Доступ к Jira

- URL вашего Jira (например: `https://company.atlassian.net`)
- Email для входа в Jira
- API Token ([создать здесь](https://id.atlassian.com/manage-profile/security/api-tokens))

---

## 🚀 Установка

### Шаг 1: Клонирование или обновление репозитория

```bash
cd /Users/exmuzzy2/jira-mcp-server

# Если уже клонирован, просто обнови
git pull origin main

# Или клонируй заново
# git clone https://github.com/yourusername/jira-mcp-server.git
# cd jira-mcp-server
```

### Шаг 2: Установка зависимостей

```bash
npm install
```

### Шаг 3: Настройка переменных окружения

Создай файл `.env` в корне проекта:

```bash
cp .env.example .env  # Если есть
# Или создай вручную:
nano .env
```

Содержимое `.env`:
```env
JIRA_BASE_URL=https://job.sbertroika.ru
JIRA_EMAIL=your.email@example.com
JIRA_API_TOKEN=your_api_token_here
LOG_LEVEL=INFO
```

### Шаг 4: Компиляция проекта

```bash
npm run build
```

**Ожидаемый результат:**
```
> @exmuzzy/exmuzzy-mcp@1.0.6 build
> npm run clean && tsc && chmod +x dist/index.js

> @exmuzzy/exmuzzy-mcp@1.0.6 clean
> rimraf dist
```

### Шаг 5: Проверка работоспособности

```bash
node dist/index.js
```

**Ожидаемый вывод:**
```
Jira MCP Server running on stdio
```

Если сервер запустился, нажми `Ctrl+C` для остановки.

---

## 🔧 Настройка в Cursor

### Вариант 1: Workspace конфигурация (Рекомендуется)

Создай файл `.cursor/mcp.json` в корне проекта:

```bash
mkdir -p .cursor
nano .cursor/mcp.json
```

Содержимое:
```json
{
  "mcpServers": {
    "jira": {
      "command": "node",
      "args": ["/Users/exmuzzy2/jira-mcp-server/dist/index.js"],
      "env": {
        "JIRA_BASE_URL": "https://job.sbertroika.ru",
        "JIRA_EMAIL": "your.email@example.com",
        "JIRA_API_TOKEN": "your_api_token_here",
        "LOG_LEVEL": "INFO"
      }
    }
  }
}
```

### Вариант 2: Глобальная конфигурация

Редактируй `~/.cursor/mcp.json`:

```bash
nano ~/.cursor/mcp.json
```

Добавь секцию `jira` в `mcpServers`.

---

## 📝 Настройка промпта в Cursor

### Шаг 1: Открой настройки Cursor

1. Открой **Cursor**
2. Нажми `Cmd + ,` (macOS) или `Ctrl + ,` (Windows/Linux)
3. Найди секцию **Rules for AI** или **Chat Rules**

### Шаг 2: Скопируй промпт

Скопируй содержимое файла `CURSOR_JIRA_PROMPT.md`:

```bash
cat CURSOR_JIRA_PROMPT.md | pbcopy  # macOS
# Или открой в редакторе и скопируй вручную
```

### Шаг 3: Вставь в настройки

Вставь промпт в поле **Rules for AI**.

### Шаг 4: Сохрани и перезапусти

1. Сохрани настройки
2. Перезапусти Cursor: `Cmd + Q` → Запусти снова

---

## ✅ Проверка установки

### Тест 1: Проверка MCP сервера

1. Открой Cursor
2. Открой чат (Cmd+L)
3. Напиши: "Покажи список доступных MCP инструментов"

**Должно быть в списке:**
- `get_my_issues_grouped` ⚡️ (новый!)
- `search_issues`
- `get_issue_details`
- `complete_issue`
- ... и другие

### Тест 2: Проверка оптимизированной функции

В Cursor чате напиши:
```
/jira
```

**Ожидаемый результат:**
- Список твоих открытых задач
- Группировка по статусам
- Прямые ссылки на Jira
- Меню быстрых действий
- Время ответа: **~1 секунда**

### Тест 3: Проверка через тестовый скрипт

```bash
node test-optimized-function.js
```

**Ожидаемый вывод:**
```
🚀 Тестирование оптимизированной функции get_my_issues_grouped

✅ Переменные окружения загружены
🔧 Инициализация Jira API клиента...
✅ Клиент инициализирован

📊 Тест 1: Традиционный метод (search_issues)
   ⏱️  Время выполнения: XXXms
   
⚡️ Тест 2: Оптимизированный метод (get_my_issues_grouped)
   ⏱️  Время выполнения: XXXms
   
📈 Сравнение результатов:
   ✅ Оптимизированный метод быстрее на XX%
   ⚡️ Ускорение: X.XXx

✅ Тестирование завершено успешно!
```

---

## 🎯 Использование

### Основные команды

| Команда | Что делает |
|---------|-----------|
| `/jira` | Показать мои открытые задачи (⚡️ оптимизировано) |
| `/jira RIVER-123` | Показать детали задачи |
| `/jira complete RIVER-123` | Закрыть задачу |
| `/jira Иванов` | Показать задачи пользователя |
| `/jira comment RIVER-123 текст` | Добавить комментарий |

### Примеры использования

**Пример 1: Просмотр своих задач**
```
Пользователь: /jira

Cursor:
📋 Мои открытые задачи

Всего открытых задач: 176
Показано: 176

### Backlog (126 задач)
- [RIVER-3953](https://job.sbertroika.ru/browse/RIVER-3953): Настроить мониторинг...
  - 📊 Backlog | High | Задача
  - 🔗 Быстрый доступ: /jira RIVER-3953
[...]
```

**Пример 2: Просмотр и закрытие задачи**
```
Пользователь: /jira RIVER-123

Cursor: [Показывает детали задачи]

Пользователь: закрой

Cursor: [Закрывает задачу с AI-генерированным резолюшеном]
```

---

## 🐛 Решение проблем

### Проблема: MCP сервер не запускается

**Причины:**
- Неверные переменные окружения
- Не установлены зависимости
- Не скомпилирован TypeScript

**Решение:**
```bash
# 1. Проверь .env файл
cat .env

# 2. Установи зависимости
npm install

# 3. Скомпилируй
npm run build

# 4. Запусти снова
node dist/index.js
```

### Проблема: Cursor не видит MCP инструменты

**Решение:**
1. Проверь конфигурацию: `cat .cursor/mcp.json`
2. Перезапусти Cursor полностью (не просто окно)
3. Проверь логи: `~/.cursor/logs/`

### Проблема: Ошибка аутентификации в Jira

**Решение:**
1. Проверь API Token: https://id.atlassian.com/manage-profile/security/api-tokens
2. Убедись, что email правильный
3. Проверь URL Jira (должен быть без слэша в конце)

### Проблема: "get_my_issues_grouped not found"

**Решение:**
1. Убедись, что код скомпилирован: `npm run build`
2. Проверь, что используешь правильную версию
3. Перезапусти Cursor

---

## 📊 Мониторинг

### Логи

Логи MCP сервера можно найти в:
```bash
~/.cursor/logs/
```

### Метрики производительности

Используй тестовый скрипт для проверки:
```bash
node test-optimized-function.js > performance.log
```

### Debug режим

Включи детальное логирование:
```bash
export LOG_LEVEL=DEBUG
node dist/index.js
```

---

## 🔄 Обновление

### Обновление до новой версии

```bash
# 1. Сохрани .env файл (если есть)
cp .env .env.backup

# 2. Обнови код
git pull origin main

# 3. Обнови зависимости
npm install

# 4. Перекомпилируй
npm run build

# 5. Восстанови .env (если нужно)
cp .env.backup .env

# 6. Перезапусти Cursor
```

---

## 📚 Дополнительные ресурсы

### Документация

- **Быстрый старт**: `CURSOR_QUICK_START.md`
- **Полный промпт**: `CURSOR_JIRA_PROMPT.md`
- **Детали оптимизации**: `OPTIMIZATION_DETAILS.md`
- **Сводка изменений**: `CHANGES_SUMMARY.md`
- **README**: `README.md`

### Полезные команды

```bash
# Запуск в dev режиме
npm run dev

# Очистка dist
npm run clean

# Полная пересборка
npm run clean && npm run build

# Проверка версии Node.js
node --version

# Проверка переменных окружения
printenv | grep JIRA
```

---

## 🎓 Лучшие практики

1. **Используй workspace конфигурацию** для разных проектов
2. **Храни API токены безопасно** (не коммить в git)
3. **Регулярно обновляй** до последней версии
4. **Используй оптимизированные функции** (`get_my_issues_grouped`)
5. **Проверяй логи** при возникновении проблем

---

## ✅ Чек-лист установки

- [ ] Node.js 18+ установлен
- [ ] Репозиторий клонирован/обновлен
- [ ] `npm install` выполнен
- [ ] `.env` файл создан с правильными данными
- [ ] `npm run build` выполнен успешно
- [ ] `.cursor/mcp.json` настроен
- [ ] Промпт добавлен в Cursor Rules
- [ ] Cursor перезапущен
- [ ] Тест `/jira` работает
- [ ] Тестовый скрипт пройден

---

## 🎉 Готово!

Теперь ты можешь использовать оптимизированную команду `/jira` в Cursor!

**Следующие шаги:**
1. Попробуй команду `/jira` в Cursor
2. Изучи все доступные команды в `CURSOR_QUICK_START.md`
3. Настрой команды под свои нужды

**Приятной работы с Jira!** 🚀

---

## 📞 Помощь

Если возникли проблемы:
1. Прочитай секцию **Решение проблем** выше
2. Проверь логи: `~/.cursor/logs/`
3. Создай issue на GitHub
4. Напиши на email: your.email@example.com

