# Оптимизированный промпт для /jira команды в Cursor

## Описание
Этот промпт оптимизирован для работы с MCP Jira сервером и выполняет команды `/jira` максимально эффективно - **за 1 запрос** вместо 6.

---

## Общее правило форматирования вывода задач

Для всех команд, выводящих информацию о задачах, используй следующий формат:

- **Ссылка на задачу**: https://job.sbertroika.ru/browse/<КЛЮЧ_ЗАДАЧИ>
- **Быстрый доступ**: `/jira <КЛЮЧ_ЗАДАЧИ>`
- **Формат списка**: "RIVER-123: summary [ссылка](https://job.sbertroika.ru/browse/RIVER-123)"
- **Детальный вывод**: Полная информация с гиперссылками для быстрого перехода в JIRA

---

## /jira без параметров ⚡️ ОПТИМИЗИРОВАНО

**ИСПОЛЬЗУЙ ТОЛЬКО:** `mcp_jira_get_my_issues_grouped` - это делает **ОДИН** запрос вместо 6!

### Что делает функция:
- Получает все открытые задачи текущего пользователя за **один запрос**
- Группирует их по статусам на сервере
- Возвращает готовый отформатированный список с:
  - Прямыми ссылками на задачи в JIRA
  - Группировкой по статусам (Backlog, Сделать, Переоткрыт, В работе, For test, и т.д.)
  - Быстрыми действиями для фильтрации
  - Подсказками по использованию

### Параметры:
- `maxResults` (опционально): максимальное количество задач (по умолчанию: 500)
- `jql` (опционально): custom JQL запрос (по умолчанию: `assignee = currentUser() AND statusCategory != Done`)

### Пример вызова:
```typescript
mcp_jira_get_my_issues_grouped()
```

---

## /jira <ключ задачи> (например, RIVER-123)

**ИСПОЛЬЗУЙ:** `mcp_jira_get_issue_details`

### Параметры:
- `issueKey`: ключ задачи (например, RIVER-123)
- `includeComments`: включить комментарии (true/false, по умолчанию: true)
- `includeWorklogs`: включить worklogs (true/false, по умолчанию: false)

### Действия после просмотра:
Предложи пронумерованный выбор действий:
1. Сформировать план автоматического выполнения задачи
2. Вернуть на автора с уточняющими вопросами
3. **В работу** - используй `mcp_jira_transition_issue` с `transitionName: "In Progress"`
4. **Выполнить** - используй `mcp_jira_transition_issue` с `transitionName: "Done"`
5. **Закрыть** - используй `mcp_jira_complete_issue`

### Пример вызова:
```typescript
mcp_jira_get_issue_details({
  issueKey: "RIVER-123",
  includeComments: true
})
```

---

## /jira <логин пользователя в JIRA>

**ИСПОЛЬЗУЙ:** `mcp_jira_search_issues`

### JQL запрос:
```sql
assignee = '<логин>' AND statusCategory != Done
```

### Пример вызова:
```typescript
mcp_jira_search_issues({
  jql: "assignee = 'ivanov' AND statusCategory != Done",
  maxResults: 100
})
```

---

## /jira <фамилия пользователя в JIRA>

**ИСПОЛЬЗУЙ:** `mcp_jira_search_users` + `mcp_jira_search_issues`

### Шаги:
1. Найти пользователей: `mcp_jira_search_users({ query: "<фамилия>" })`
2. Если найден один - показать его задачи через `mcp_jira_search_issues`
3. Если несколько - предложить выбор

### Пример:
```typescript
// Шаг 1
mcp_jira_search_users({ query: "Иванов" })

// Шаг 2 (если найден один)
mcp_jira_search_issues({
  jql: "assignee = 'ivanov' AND statusCategory != Done"
})
```

---

## /jira <название эпика>

**ИСПОЛЬЗУЙ:** `mcp_jira_search_issues`

### JQL запрос:
```sql
issuetype = Epic AND summary ~ '<название>'
```

### Пример:
```typescript
mcp_jira_search_issues({
  jql: "issuetype = Epic AND summary ~ 'API'"
})
```

---

## /jira sprint

**ИСПОЛЬЗУЙ:** `mcp_jira_get_boards` + `mcp_jira_get_board_issues`

### Шаги:
1. Получить доски: `mcp_jira_get_boards()`
2. Получить задачи из активного спринта: `mcp_jira_get_board_issues`

### Пример:
```typescript
mcp_jira_get_board_issues({
  boardId: "123",
  assigneeFilter: "currentUser",
  statusFilter: "all"
})
```

---

## /jira rss

**ИСПОЛЬЗУЙ:** `mcp_jira_search_issues`

### JQL запрос:
```sql
ORDER BY updated DESC
```

### Пример:
```typescript
mcp_jira_search_issues({
  jql: "ORDER BY updated DESC",
  maxResults: 20
})
```

---

## /jira boards

**ИСПОЛЬЗУЙ:** `mcp_jira_get_boards`

### Пример:
```typescript
mcp_jira_get_boards()
```

---

## /jira board <boardId>

**ИСПОЛЬЗУЙ:** `mcp_jira_get_board_details`

### Пример:
```typescript
mcp_jira_get_board_details({ boardId: "123" })
```

---

## /jira projects

**ИСПОЛЬЗУЙ:** `mcp_jira_get_projects`

### Пример:
```typescript
mcp_jira_get_projects()
```

---

## /jira create <projectKey> <summary>

**ИСПОЛЬЗУЙ:** `mcp_jira_create_issue`

### Параметры:
- `projectKey`: ключ проекта (обязательно)
- `issueType`: тип задачи (обязательно, например: Task, Bug, Story)
- `summary`: заголовок (обязательно)
- `description`: описание (опционально)
- `priority`: приоритет (опционально: Highest, High, Medium, Low, Lowest)
- `assignee`: исполнитель (опционально)
- `labels`: метки (опционально)

### Пример:
```typescript
mcp_jira_create_issue({
  projectKey: "RIVER",
  issueType: "Task",
  summary: "Новая задача",
  description: "Описание задачи",
  priority: "High"
})
```

---

## /jira comment <ключ> <текст комментария>

**ИСПОЛЬЗУЙ:** `mcp_jira_add_comment`

### Пример:
```typescript
mcp_jira_add_comment({
  issueKey: "RIVER-123",
  comment: "Работа выполнена"
})
```

---

## /jira transition <ключ> <новый статус>

**ИСПОЛЬЗУЙ:** `mcp_jira_transition_issue`

### Параметры:
- `issueKey`: ключ задачи
- `transitionName`: название перехода (например: "In Progress", "Done", "Готово")
- `comment`: комментарий (опционально)

### Пример:
```typescript
mcp_jira_transition_issue({
  issueKey: "RIVER-123",
  transitionName: "In Progress",
  comment: "Начал работу"
})
```

---

## /jira worklog <ключ> <время>

**ИСПОЛЬЗУЙ:** `mcp_jira_add_worklog`

### Параметры:
- `issueKey`: ключ задачи
- `timeSpent`: время работы (формат: "2h 30m", "1d", "4h")
- `comment`: описание работы (опционально)
- `startDate`: дата начала (опционально, ISO формат)

### Пример:
```typescript
mcp_jira_add_worklog({
  issueKey: "RIVER-123",
  timeSpent: "2h 30m",
  comment: "Реализация API"
})
```

---

## /jira hierarchy <ключ>

**ИСПОЛЬЗУЙ:** `mcp_jira_get_issue_hierarchy_from_root`

### Параметры:
- `issueKey`: ключ задачи для построения иерархии
- `maxDepth`: максимальная глубина (по умолчанию: 10)
- `includeEpic`: включить эпик (по умолчанию: false)

### Пример:
```typescript
mcp_jira_get_issue_hierarchy_from_root({
  issueKey: "RIVER-123",
  maxDepth: 10,
  includeEpic: true
})
```

---

## /jira links <ключ>

**ИСПОЛЬЗУЙ:** `mcp_jira_get_issue_links`

### Пример:
```typescript
mcp_jira_get_issue_links({
  issueKey: "RIVER-123"
})
```

---

## /jira update <ключ>

**ИСПОЛЬЗУЙ:** `mcp_jira_update_issue`

### Параметры:
- `issueKey`: ключ задачи (обязательно)
- `summary`: новый заголовок (опционально)
- `description`: новое описание (опционально)
- `priority`: новый приоритет (опционально)
- `assignee`: новый исполнитель (опционально)
- `labels`: новые метки (опционально)

### Пример:
```typescript
mcp_jira_update_issue({
  issueKey: "RIVER-123",
  summary: "Обновленный заголовок",
  priority: "High"
})
```

---

## Дополнительные команды

### /jira complete <ключ> (Закрыть задачу)

**ИСПОЛЬЗУЙ:** `mcp_jira_complete_issue`

Автоматически переводит задачу в статус "Готово" с AI-генерируемым резолюшеном.

### Пример:
```typescript
mcp_jira_complete_issue({
  issueKey: "RIVER-123",
  customResolution: "Задача выполнена успешно"  // опционально
})
```

---

## Важные замечания

### ⚡️ Оптимизация производительности

1. **Для просмотра своих задач**: ВСЕГДА используй `mcp_jira_get_my_issues_grouped` вместо `mcp_jira_search_issues`
   - ✅ Делает 1 запрос вместо 6
   - ✅ Группирует задачи на сервере
   - ✅ Возвращает готовый форматированный результат

2. **Для поиска задач**: используй `mcp_jira_search_issues` с правильным JQL
   - Задачи пользователя: `assignee = currentUser() AND statusCategory != Done`
   - Задачи другого пользователя: `assignee = 'username' AND statusCategory != Done`
   - Эпики: `issuetype = Epic AND summary ~ 'text'`
   - Последние обновленные: `ORDER BY updated DESC`

3. **Для работы с конкретной задачей**: используй `mcp_jira_get_issue_details`
   - Всегда включай `includeComments: true` для полной информации
   - Задача автоматически запоминается как последняя просмотренная

### 🎯 Команды быстрого доступа

Всегда добавляй в вывод:
- Прямые ссылки на задачи: `https://job.sbertroika.ru/browse/RIVER-123`
- Быстрый доступ через команду: `/jira RIVER-123`
- Меню действий с нумерованным списком

### 📊 Статусы задач

Основные статусы, которые нужно отслеживать:
- **Backlog** - задачи в бэклоге
- **Сделать** - задачи к выполнению
- **Переоткрыт** - переоткрытые задачи
- **В работе** - задачи в процессе выполнения
- **For test** - задачи на тестировании
- **Тестирование в процессе** - в процессе тестирования
- **On hold** - задачи на паузе
- **Blocked** - заблокированные задачи
- **Under Review** - задачи на проверке

---

## Примеры использования

### Пример 1: Просмотр своих задач
```
Пользователь: /jira
Ответ: [Вызов mcp_jira_get_my_issues_grouped()]
```

### Пример 2: Просмотр задачи
```
Пользователь: /jira RIVER-123
Ответ: [Вызов mcp_jira_get_issue_details({ issueKey: "RIVER-123", includeComments: true })]
```

### Пример 3: Закрыть задачу
```
Пользователь: /jira complete RIVER-123
Ответ: [Вызов mcp_jira_complete_issue({ issueKey: "RIVER-123" })]
```

### Пример 4: Найти задачи коллеги
```
Пользователь: /jira Иванов
Ответ: 
1. [Вызов mcp_jira_search_users({ query: "Иванов" })]
2. [Если найден один] [Вызов mcp_jira_search_issues({ jql: "assignee = 'ivanov' AND statusCategory != Done" })]
```

---

## Итоговая производительность

### До оптимизации:
- `/jira` без параметров: **6 запросов**
- Время ответа: ~3-5 секунд

### После оптимизации:
- `/jira` без параметров: **1 запрос** ⚡️
- Время ответа: ~0.5-1 секунда
- **Ускорение в 5-6 раз!**

---

## Контакты и поддержка

Для вопросов и предложений по улучшению функционала MCP сервера:
- GitHub: [jira-mcp-server](https://github.com/yourusername/jira-mcp-server)
- Email: your.email@example.com

