# Быстрый старт: /jira команда в Cursor

## 🚀 Главное - Используй оптимизированную функцию!

Для команды `/jira` без параметров **ВСЕГДА** используй:

```typescript
mcp_jira_get_my_issues_grouped()
```

**Почему?**
- ⚡️ **1 запрос** вместо 6
- 🎯 Автоматическая группировка по статусам
- 🔗 Прямые ссылки на задачи
- 📊 Готовое форматирование

---

## 📋 Быстрая справка по командам

| Команда | MCP Функция | Описание |
|---------|-------------|----------|
| `/jira` | `mcp_jira_get_my_issues_grouped()` | Все твои открытые задачи (⚡️ оптимизировано) |
| `/jira RIVER-123` | `mcp_jira_get_issue_details({ issueKey: "RIVER-123", includeComments: true })` | Детали задачи |
| `/jira complete RIVER-123` | `mcp_jira_complete_issue({ issueKey: "RIVER-123" })` | Закрыть задачу |
| `/jira Иванов` | `mcp_jira_search_users({ query: "Иванов" })` → `mcp_jira_search_issues(...)` | Задачи пользователя |
| `/jira comment RIVER-123 текст` | `mcp_jira_add_comment({ issueKey: "RIVER-123", comment: "текст" })` | Добавить комментарий |
| `/jira transition RIVER-123 "In Progress"` | `mcp_jira_transition_issue({ issueKey: "RIVER-123", transitionName: "In Progress" })` | Сменить статус |

---

## 🔥 Примеры

### Просмотр своих задач
```
Пользователь: /jira

AI вызывает:
mcp_jira_get_my_issues_grouped()

Результат:
✅ 176 задач за 1 запрос
✅ Группировка по статусам
✅ Прямые ссылки
✅ Меню быстрых действий
```

### Просмотр и закрытие задачи
```
Пользователь: /jira RIVER-123

AI вызывает:
mcp_jira_get_issue_details({
  issueKey: "RIVER-123",
  includeComments: true
})

Затем пользователь: закрой

AI вызывает:
mcp_jira_complete_issue({
  issueKey: "RIVER-123"
})
```

### Найти задачи коллеги
```
Пользователь: /jira Иванов

AI вызывает последовательно:
1. mcp_jira_search_users({ query: "Иванов" })
2. mcp_jira_search_issues({
     jql: "assignee = 'ivanov' AND statusCategory != Done"
   })
```

---

## ⚙️ Настройка в Cursor

### 1. Добавь промпт в Chat Rules

Скопируй содержимое файла `CURSOR_JIRA_PROMPT.md` в настройки Cursor:
- Открой **Settings** → **Cursor Settings** → **Rules for AI**
- Вставь промпт из `CURSOR_JIRA_PROMPT.md`

### 2. Проверь MCP конфигурацию

Убедись, что в `~/.cursor/mcp.json` или workspace-конфиге есть:

```json
{
  "mcpServers": {
    "jira": {
      "command": "node",
      "args": ["/path/to/jira-mcp-server/dist/index.js"],
      "env": {
        "JIRA_BASE_URL": "https://your-jira.atlassian.net",
        "JIRA_EMAIL": "your-email@example.com",
        "JIRA_API_TOKEN": "your-api-token"
      }
    }
  }
}
```

### 3. Перезапусти Cursor

Перезапусти Cursor для применения изменений.

---

## 🎯 Лучшие практики

### ✅ DO

- **Используй** `mcp_jira_get_my_issues_grouped()` для просмотра своих задач
- **Включай** `includeComments: true` при просмотре детальной информации
- **Группируй** связанные операции в один запрос где возможно
- **Используй** прямые ссылки формата `https://job.sbertroika.ru/browse/RIVER-123`

### ❌ DON'T

- **Не используй** `mcp_jira_search_issues` для просмотра своих задач (используй `get_my_issues_grouped`)
- **Не делай** множество мелких запросов если можно сделать один большой
- **Не забывай** включать комментарии при просмотре детальной информации

---

## 📊 Производительность

### Сравнение

| Операция | Старый способ | Новый способ | Ускорение |
|----------|---------------|--------------|-----------|
| Просмотр своих задач | 6 запросов (~3-5 сек) | 1 запрос (~0.5-1 сек) | **5-6x** ⚡️ |
| Детали задачи | 1 запрос | 1 запрос | - |
| Поиск пользователей | 1-2 запроса | 1-2 запроса | - |

---

## 🐛 Troubleshooting

### Проблема: MCP не отвечает
**Решение:**
1. Проверь логи: `~/.cursor/logs/`
2. Убедись что MCP сервер запущен
3. Проверь переменные окружения в конфигурации

### Проблема: Не находит задачи
**Решение:**
1. Проверь JQL запрос в `mcp_jira_search_issues`
2. Убедись что у пользователя есть права на просмотр задач
3. Проверь что задачи не в статусе Done

### Проблема: Медленно работает
**Решение:**
1. Используй `mcp_jira_get_my_issues_grouped()` вместо `search_issues`
2. Уменьши `maxResults` если результатов слишком много
3. Используй более конкретные JQL запросы

---

## 📚 Дополнительная документация

- Полный промпт: `CURSOR_JIRA_PROMPT.md`
- README проекта: `README.md`
- Примеры использования: `examples/`

---

## 🎉 Готово!

Теперь ты можешь эффективно работать с Jira прямо из Cursor!

**Главное помни:**
- `/jira` → `mcp_jira_get_my_issues_grouped()` ⚡️
- Всегда используй оптимизированные функции
- Группируй запросы где возможно

