# 📋 Makefile Cheat Sheet

## Быстрый старт

```bash
# Показать все команды
make help

# Запустить все тесты
make test-all
```

## 🔧 Разработка

```bash
make install              # Установить зависимости
make build                # Собрать проект
make clean                # Очистить build
make dev                  # Запустить dev сервер с hot reload
make start                # Собрать и запустить production

make lint                 # Проверить код линтером
make lint-fix             # Исправить проблемы линтера
make format               # Отформатировать код
make format-check         # Проверить форматирование
make typecheck            # Проверить типы TypeScript
```

## ✅ Тестирование

### Основные тесты

```bash
make test-all             # Запустить все тесты
make check-env            # Проверить переменные окружения
make test-connection      # Проверить подключение к Jira
```

### Тесты MCP функций

```bash
# Тест оптимизированной функции (1 запрос вместо 6)
make test-grouped         # Предпросмотр (первые 100 строк)
make test-grouped-full    # Полный вывод

# Тест приоритетов
make test-priorities      # Показать все приоритеты Jira с цветами

# Тест текущего пользователя
make test-current-user    # Получить информацию о текущем пользователе

# Тест моих задач
make test-my-issues       # Показать мои открытые задачи (10 шт)

# Тест конкретной задачи
make test-issue-detail ISSUE_KEY=RIVER-123

# Тест поиска пользователей
make test-search-users QUERY=Агафонов

# Тест досок
make test-boards          # Показать все доски

# Тест проектов
make test-projects        # Показать все проекты
```

## 📊 Просмотр логов

```bash
# Просмотр логов в реальном времени (macOS)
make logs                 

# Список всех директорий с логами
make logs-list

# Показать только ошибки
make logs-errors
```

## 🔍 Git операции

```bash
make git-status           # git status
make git-log              # git log --oneline -10
make git-diff             # git diff
```

## 📦 Публикация

```bash
# Изменить версию
make version-patch        # 1.0.8 -> 1.0.9
make version-minor        # 1.0.8 -> 1.1.0
make version-major        # 1.0.8 -> 2.0.0

# Опубликовать в npm
make publish              # npm publish (с подтверждением)
```

## 🐳 Docker (опционально)

```bash
make docker-build         # Собрать Docker образ
make docker-run           # Запустить Docker контейнер
```

## 💡 Примеры использования

### Проверить работу MCP сервера

```bash
# 1. Проверить окружение
make check-env

# 2. Протестировать подключение
make test-connection

# 3. Получить мои задачи
make test-grouped

# 4. Посмотреть детали конкретной задачи
make test-issue-detail ISSUE_KEY=RIVER-3502
```

### Разработка и тестирование

```bash
# 1. Внести изменения в код
# 2. Собрать проект
make build

# 3. Проверить типы
make typecheck

# 4. Проверить линтер
make lint

# 5. Отформатировать код
make format

# 6. Запустить тесты
make test-all

# 7. Закоммитить
git add .
git commit -m "feat: new feature"
git push
```

### Отладка проблем

```bash
# 1. Посмотреть ошибки в логах
make logs-errors

# 2. Посмотреть логи в реальном времени
make logs

# 3. Проверить конкретную задачу
make test-issue-detail ISSUE_KEY=RIVER-123

# 4. Проверить текущего пользователя
make test-current-user
```

## 🎨 Цветовая кодировка

- 🔵 **Синий** - заголовки разделов
- 🟡 **Желтый** - названия команд
- 🟢 **Зеленый** - успешные операции
- 🔴 **Красный** - ошибки

## 📝 Требования

- **GNU Make** (обычно уже установлен на macOS/Linux)
- **Node.js** >= 16.0.0
- **npm**
- **Файл .env** с переменными окружения:
  ```
  JIRA_BASE_URL=https://your-jira.com
  JIRA_BEARER_TOKEN=your-token
  ```

## 🆘 Помощь

Если команда не работает:

1. Проверьте, что `.env` файл существует и содержит все необходимые переменные
2. Запустите `make check-env` для проверки окружения
3. Посмотрите логи: `make logs-errors`
4. Проверьте статус: `make test-connection`

Для получения полного списка команд:
```bash
make help
```

